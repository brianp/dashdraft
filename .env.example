# Application
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Database (PostgreSQL connection string)
DATABASE_URL=postgresql://user:password@localhost:5432/dashdraft?schema=public

# GitHub App Credentials
# Create a GitHub App at https://github.com/settings/apps
GITHUB_APP_ID=123456
GITHUB_APP_CLIENT_ID=Iv1.xxxxxxxxxxxxxxxx
GITHUB_APP_CLIENT_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# The private key (PEM format) - can be a file path OR the key content
# Option 1: File path (recommended - easier to manage)
GITHUB_APP_PRIVATE_KEY=./dashdraft.pem
# Option 2: Inline key with \n for newlines
# GITHUB_APP_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\nMIIE...\n-----END RSA PRIVATE KEY-----"
GITHUB_WEBHOOK_SECRET=your-webhook-secret

# Session
# Generate with: openssl rand -base64 32
SESSION_SECRET=your-session-secret-at-least-32-characters-long

# AI Features (Vercel AI Gateway)
# Get your gateway key from Vercel Dashboard > AI
# The SDK automatically routes through Vercel's gateway when this key is set
AI_GATEWAY_API_KEY=your-vercel-ai-gateway-api-key
# Set to 'true' to enable AI features
NEXT_PUBLIC_AI_ENABLED=false
# Optional: Disable individual AI features (both enabled by default when AI is enabled)
# NEXT_PUBLIC_AI_KICKSTART_ENABLED=true  # Help users start new documents
# NEXT_PUBLIC_AI_ASSIST_ENABLED=true     # Help users who appear stuck
